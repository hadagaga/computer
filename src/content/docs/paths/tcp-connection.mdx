---
title: 一次 TCP 连接背后发生了什么
description: 从应用程序调用 Socket 开始，理解 TCP 与 IP 如何共同完成一次网络连接。
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import StatusBadge from '../../../components/StatusBadge.astro';

<StatusBadge status="planned" />

这条路径从应用程序发起通信的动作开始，先建立 Socket、TCP 与 IP 的主干关系，再进入连接管理、可靠传输和安全分支。

## 路径目标

完成这条路径后，应能解释：一段应用代码发起连接时，操作系统协议栈承担了哪些职责；TCP 为什么依赖 IP；连接建立、可靠传输和安全风险分别出现在哪里。

## 主干

<div class="path-mainline not-content">
	<div class="path-step"><strong><a href="/nodes/socket/">1. Socket</a></strong><p>应用程序进入协议栈的接口。</p></div>
	<div class="path-step"><strong><a href="/nodes/tcp/">2. TCP</a></strong><p>建立连接并提供可靠字节流。</p></div>
	<div class="path-step"><strong><a href="/nodes/ip/">3. IP</a></strong><p>完成寻址与跨网络转发。</p></div>
</div>

## 从 TCP 展开的分支

<CardGrid>
	<Card title="连接生命周期" icon="seti:clock">
		三次握手建立状态，四次挥手分别关闭双向连接。
	</Card>
	<Card title="可靠传输机制" icon="approve-check-circle">
		重传、滑动窗口和拥塞控制共同处理丢包、接收能力与网络负载。
	</Card>
	<Card title="安全与观察" icon="shield">
		通过 [SYN Flood](/nodes/syn-flood/) 理解机制滥用，并使用 Wireshark 观察真实报文。
	</Card>
</CardGrid>

## 下一步

- 从 [TCP 节点](/nodes/tcp/) 查看全部一跳关联。
- 打开 [TCP 周边知识网](/graph/tcp/) 横向探索机制和安全节点。
