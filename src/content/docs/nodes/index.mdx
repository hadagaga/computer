---
title: 节点索引
description: 查看计算机体系视频百科第一批知识节点及其内容状态。
---

import { Badge, Card, CardGrid } from '@astrojs/starlight/components';
import nodes from '../../../data/nodes.json';

节点是可独立理解和持续更新的知识单元。第一阶段先围绕 TCP 建立网络与安全节点。

<CardGrid>
	{nodes.map((node) => (
		<Card title={node.title}>
			<Badge text="规划中" variant="note" size="small" />
			<p>{node.summary}</p>
			{node.url ? <a href={node.url}>查看节点 →</a> : <span>内容页待建立</span>}
		</Card>
	))}
</CardGrid>
